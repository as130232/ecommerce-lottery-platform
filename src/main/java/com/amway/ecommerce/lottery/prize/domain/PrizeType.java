package com.amway.ecommerce.lottery.prize.domain;

public enum PrizeType {
    /** A real prize that consumes stock. */
    PRIZE,
    /** 銘謝惠顧 - the no-prize option, never consumes stock. */
    THANKS
}
